// Código não executável

// Linguagens não-estruturadas (go to) x Linguagens estruturadas

// Paradigmas da linguagem estruturada
// Procedural - funções e procedimentos que recebem um conjunto de dados e processam esses dados
processamento(valor1, valor2, valor3);

// Orientação a objeto - estrutura que gera o objeto e o objeto em si. O objeto possui seus dados(atributos) e comportamentos(métodos)
// Mais próximo do mundo real
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        // ...
    }
}

objeto.processamento(); // Podemos acessar os dados daquele objeto em específico

/* Princípios importantes
 * 1. Abstração 
 * 2. Encapsulamento
 * 3. Herança (prototype)
 * 4. Polimorfismo
 */