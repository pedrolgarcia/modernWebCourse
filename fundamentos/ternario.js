// Operador ternário
const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado'; // condição ? true : false

console.log(resultado(7.1)); // > 7 aprovado
console.log(resultado(6.7)); // < 7 reprovado