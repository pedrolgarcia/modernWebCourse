{ // arquivo de configuração ts
    "files": [
        "src/**/*.ts" // arquivos do projeto
    ],
    "compilerOptions": {
        "rootDir": "src", 
        "noImplicitAny": true, // obriga a declararmos any quando a variável não tiver nenhum tipo
        "target": "es5" // versão do transpile
    }
}